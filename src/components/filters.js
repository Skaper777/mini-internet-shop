import AbstractComponent from './abstract';
/**
 * Класс шаблона для фильтров
 */
export default class Filters extends AbstractComponent {
  constructor() {
    super();
  }

  getTemplate() {
    return `

    `;
  }
}
